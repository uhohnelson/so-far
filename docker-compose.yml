services:
  bot:
    build: .
    container_name: sofar-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////data/sofar.db
    volumes:
      - sofar-data:/data

volumes:
  sofar-data:
